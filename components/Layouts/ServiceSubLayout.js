import React from 'react'
import Card from '../Cards/Card'

export default function SeriviceSubLayout({services}) {
  console.log(services,'servicesservices');
  return (
    <div>
      {/* {Object?.values(services).length>0?Object.values(services).map((s,i)=>{ */}
        {/* // console.log(s,'ssssssssssssssssss',i);
              return( */}
                <>
              {/* <Card data={s}/> */}
              </>
              {/* ) */}
            {/* }):''} */}

              </div>
              
  )
}
